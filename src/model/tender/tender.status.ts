export enum TenderType {
    PUBLIC = "PUBLIC",
    INVITATION = "INVITATION",
    RESTRICTED = "RESTRICTED",
}

export enum TenderStatus {
    DRAFT = "DRAFT",
    PUBLISHED = "PUBLISHED",
    CLOSED = "CLOSED",
    CANCELLED = "CANCELLED",
}
